type: object
properties:
  enable:
    type: array
    items:
      type: string
      format: uuid
      description: Match ID to enable.
  disable:
    type: array
    items:
      type: string
      format: uuid
      description: Match ID to disable.
