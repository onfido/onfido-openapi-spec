required:
  - id
  - name
allOf:
  - $ref: definitions.yaml#/common
  - type: object
    properties:
      breakdown:
        $ref: known_faces_breakdown.yaml
      properties:
        $ref: known_faces_properties.yaml
