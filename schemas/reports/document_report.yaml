required:
  - id
  - name
allOf:
  - $ref: definitions.yaml#/report_shared
  - type: object
    properties:
      breakdown:
        $ref: document_breakdown.yaml
      properties:
        $ref: document_properties.yaml
