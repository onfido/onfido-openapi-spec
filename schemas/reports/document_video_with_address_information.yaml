required:
  - id
  - name
allOf:
  - $ref: definitions.yaml#/common
  - type: object
    properties:
      breakdown:
        $ref: document_breakdown.yaml
      properties:
        $ref: document_properties.yaml
