required:
  - id
  - name
allOf:
  - $ref: definitions.yaml#/common
  - type: object
    properties:
      breakdown:
        $ref: watchlist_aml_breakdown.yaml
      properties:
        $ref: watchlist_aml_properties.yaml
