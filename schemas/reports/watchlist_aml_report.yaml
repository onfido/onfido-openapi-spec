allOf:
  - $ref: definitions.yaml#/report_shared
  - type: object
    properties:
      breakdown:
        $ref: watchlist_aml_breakdown.yaml
      properties:
        $ref: watchlist_aml_properties.yaml
