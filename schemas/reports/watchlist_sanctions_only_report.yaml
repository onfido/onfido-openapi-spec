allOf:
  - $ref: report_common.yaml
  - type: object
    properties:
      name:
        type: string
        enum: [watchlist_sanctions_only]
  - type: object
    properties:
      breakdown:
        $ref: watchlist_standard_breakdown.yaml
      properties:
        $ref: watchlist_standard_properties.yaml
