required:
  - id
  - name
allOf:
  - $ref: definitions.yaml#/common
  - type: object
    properties:
      breakdown:
        $ref: identity_enhanced_breakdown.yaml
      properties:
        $ref: identity_enhanced_properties.yaml
