required:
  - id
  - name
allOf:
  - $ref: definitions.yaml#/common
  - type: object
    properties:
      breakdown:
        $ref: proof_of_address_breakdown.yaml
      properties:
        $ref: proof_of_address_properties.yaml
